import { Address, Company } from "../usersAPI";

export default interface IUserData {
  id: number;
  name: string;
  username: string;
  email: string;
  address: Address;
  phone: string;
  website: string;
  company: Company;
}